export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-3xl font-serif font-semibold text-[#0b4726]">
        Dashboard
      </h1>
      <p className="mt-2 text-black/60">
        You’re logged in. Next we’ll load the current user and build the app UI.
      </p>
    </div>
  );
}
